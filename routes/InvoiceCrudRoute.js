const router = require("express").Router();

router.get("/", () => {
  //
});

router.post("/", () => {
  //
});

router.put("/", () => {
  //
});

router.delete("/", () => {
  //
});

module.exports = router;
